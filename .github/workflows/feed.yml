name: KuCoin Solayer feed

on:
  schedule:
    - cron: '*/5 * * * *'        # elke 5 minuten
  workflow_dispatch:

jobs:
  push-feed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'   # 3.11 is lichter & sneller op GH-runners

      # ▼ install nu ALLE packages die het script nodig heeft
      - name: Install dependencies
        run: pip install --upgrade pip && pip install requests pandas ta

      - name: Run feed-script
        run: python kucoin_solayer_feed.py
        env:
          GIST_TOKEN: ${{ secrets.GIST_TOKEN }}
          GIST_ID:  cae271a238f04de0fe7359f03490bc5b
